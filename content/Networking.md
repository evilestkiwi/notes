`ip a` or `ip add` to find info on your computer's IP

website is my Dad, IP address is my Dad's phone number, the dns server is my phonebook

`dig [domain]` to find IP addresses of a domain

dhcp is the central networking config from router/server

tcp is a way of sending network packets, it is more precise/self-checking but a bit slower

udp is a way of sending network packets, but does not check if all the data is delivered

`nmap -p [port] [ip]` to see what is on each port

### IP Addresses
127 is personal computer IP/home IP

vlans is digitally separating networks using the same hardware

subnets are a group of IP addresses, typically grouped by their block of 4